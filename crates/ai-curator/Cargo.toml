[package]
name = "ai-curator"
version = "0.1.0"
edition = "2024"

[dependencies]
outfit-generator = { path = "../outfit-generator" }
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
